apiVersion: v1
kind: Service
metadata:
  name: {{ include "hub-commit-valid-fail.fullname" . }}
  labels:
    {{- include "hub-commit-valid-fail.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hub-commit-valid-fail.selectorLabels" . | nindent 4 }}
